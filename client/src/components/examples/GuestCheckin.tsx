import GuestCheckin from "@/pages/GuestCheckin";
import { ThemeProvider } from "../ThemeProvider";

export default function GuestCheckinExample() {
  return (
    <ThemeProvider>
      <GuestCheckin />
    </ThemeProvider>
  );
}
